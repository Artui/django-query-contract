"""Reading a capture back: detectors, groupings, and the growth harness.

The verbs are the contract. ``find_`` produces findings and keys on the whole
call stack; ``group_by_`` re-files the same records under a display rule and
claims nothing; ``measure_`` runs the same work at several sizes and reports the
curve, and only ``assert_`` turns any of it into a failure.
"""
