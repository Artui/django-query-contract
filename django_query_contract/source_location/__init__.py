"""Where a statement came from, and how that address is shown.

One place decides which frames are kept, one decides whether a frame is code the
reader can edit, and one decides how a path is written down. A record, a finding
and an attribution disagreeing about where a statement came from is worse than
any of them having no answer, so these are shared rather than forked.
"""
